[pytest]
# tests/ — API & integration; tests/unit/ — module-level unit tests (formerly enterprise_rag/tests)
testpaths = tests
pythonpath = enterprise_rag/src
markers =
    integration: live API tests (require running server)
