# RAG core
langchain>=0.2.14,<0.3
langchain-community>=0.2.12,<0.3
langchain-text-splitters>=0.2.0,<0.3
# Pin to langchain-core 0.2.x; do not use langchain-openai 1.x (requires langchain-core 2.x).
langchain-openai>=0.1.8,<0.3
langgraph>=0.2.14,<0.3

# plan1: milvus-lite on Linux/macOS only; Windows uses in-process NumPy vector store (milvus_indexer).
milvus-lite>=2.4.0; sys_platform != "win32"
pymilvus>=2.4.7,<2.5

# Step 1 / 3 / 4: parsing + BGE + reranker
FlagEmbedding>=1.2.10
# ModelScope for model downloads (set USE_MODELSCOPE_DOWNLOAD=true in .env)
modelscope>=1.14.0
llama-parse>=0.5.0
# Avoid unstructured[pdf] extras (onnx/opencv); PDF parsing uses pdfplumber first (document_loader/parser.py).
unstructured>=0.15.7
pdfplumber>=0.11.0

# Fallback embedder
sentence-transformers>=3.0.0
torch>=2.2.0

openai>=1.44.0
fastapi>=0.114.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.1
jieba>=0.42.1
rank-bm25>=0.2.2
tiktoken>=0.7.0
streamlit>=1.38.0
httpx>=0.27.0
presidio-analyzer>=2.2.354
presidio-anonymizer>=2.2.354
ragas>=0.2.2
langsmith>=0.1.140
datasets>=2.21.0
pytest>=8.0
# If ragas import fails with numpy/pandas dtype mismatch, reinstall matching wheels:
#   pip install -U "numpy>=1.26,<2.2" "pandas>=2.0,<2.2"
